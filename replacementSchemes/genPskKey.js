var module = require('module')
var Guid = require('guid');

var conf = require('../modules/config');

var replacementScheme = {'indicator': 'PSK_REPLACE_INDICATOR', 'value': 'ciP$ss'};

module.exports = {'replacementScheme': replacementScheme}